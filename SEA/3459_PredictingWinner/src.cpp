#include <iostream>
using namespace std;

int dfs(int n, int x, bool person){
     int next1=2*x;
     int next2=2*x+1;
     if(2*now<n && (2*now+1))
}
int main(){
     int t;
     cin >> t;

     for(int tc=1; tc<=t; tc++){
          long long int n;
          cin >> n;

          dfs(n, 1, 0);
          // 0은 alice , 1은 bob
     }
}
